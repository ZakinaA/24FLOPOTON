<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% block body %}{% endblock %}
        {% block javascripts %}{% endblock %}
		
		LISTE DES JOURS</br>
		<table>
		{% for j in pJour %}
		<tr>
			<td><a href="{{ path('app_jour_consulter', { 'id': j.id }) }}">{{ j.libelle }}</a></td>
	{% else %}
		<tr>
		<td>Aucun jour n'a été trouvé.</td>
		</tr>
	{% endfor %}
</table>
	
	 
    </body>
</html>